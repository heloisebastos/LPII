package ifmt.cba.apps;


/* import ifmt.cba.util.EntityManagerUtil;
import ifmt.cba.vo.Produto;
import jakarta.persistence.EntityManager;


A CLASSE NÃO ESTÁ SENDO USADA !!!!!!!!!!!! 

public class AppProdutoItemVenda {  //mapeamento itemvenda N->1 produto 
   /*  public static void main (String[]args){
        try {
              EntityManager em = EntityManagerUtil.getEntityManager();
              em.getTransaction().begin();

              Produto newProduto = new Produto("macarrao",0);
              em.persist(newProduto);

              newProduto = new Produto("salame",0);
              em.persist(newProduto);

              newProduto = new Produto("bacalhau",0);
              em.persist(newProduto);
              
              em.getTransaction().commit();

        } catch (Exception e) {
            System.out.println(e.toString());
        }
    }  
} */
