package ifmt.cba.apps;

  // NÃO PRECISA DESTA CLASSE

/*import java.util.Calendar;
import ifmt.cba.util.EntityManagerUtil;
import ifmt.cba.vo.Venda;
import jakarta.persistence.EntityManager;

//Mapeamento Vendedor 1->N  Venda
public class AppVendaVendedor { //App Venda_Vendedor
    public static void main (String []args){
        try {
            EntityManager em = EntityManagerUtil.getEntityManager();
            em.getTransaction().begin();

            Venda data = new Venda(Calendar.getInstance());
            em.persist(data);

            data = new Venda(Calendar.getInstance());
            em.persist(data);
          
            data = new Venda(Calendar.getInstance());
            em.persist(data);
          
            em.getTransaction().commit();
        } catch (Exception e) {
            System.out.println(e.toString());
         }
    } 
} */
